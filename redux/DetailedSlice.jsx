import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const DetailedSlice = createSlice({
  name: 'component',
  initialState,
  reducers: {}
});

export const {} = DetailedSlice.actions

export default DetailedSlice.reducer